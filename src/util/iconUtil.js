import React from 'react';
import paths from 'material-design-icons-svg/paths';
// import loadIcons from 'material-design-icons-svg';

// const icons = loadIcons(paths);

export function getSvg({name}) {
  const path = paths[name];
}